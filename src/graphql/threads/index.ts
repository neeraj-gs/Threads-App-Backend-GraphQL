//export all of themm

import {typeDefs} from './typedef'
import {queries} from './queries'
import {mutations} from './mutations'
import {resolvers} from './resolvers'

export const Threads = {typeDefs, queries, mutations, resolvers}